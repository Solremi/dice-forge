[core]
	engine = pg
	top_dir = migrations
	# plan_file = migrations/sqitch.plan
[engine "pg"]
target = db:pg://solremi:solremi@localhost/diceforge
	# registry = sqitch
	# client = psql

	#target = db:pg://diceforge:diceforge@localhost/diceforge
